<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Получатель action-кнопок из уведомлений.
             exported=false — вызываем только из наших PendingIntent.
             intent-filter с action'ами — для явного соответствия
             (хотя явный класс-компонент уже достаточен). -->
        <receiver
            android:name=".worker.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.ACTION_PAYMENT_RECEIVED" />
                <action android:name="com.example.ACTION_REMIND_IN_ONE_HOUR" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
