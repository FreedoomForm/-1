package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ── Основная палитра (модернизированная Claude-тема с янтарным акцентом) ────
val ClaudeBackground = Color(0xFFFAFAF9)        // тёплый off-white
val ClaudeCard = Color(0xFFFFFFFF)
val ClaudeText = Color(0xFF18181B)              // near-black
val ClaudeTextSecondary = Color(0xFF71717A)     // muted gray
val ClaudeAccent = Color(0xFFD97706)            // amber-600 — тёплый акцент
val ClaudeAccentMuted = Color(0xFFFED7AA)       // amber-200
val ClaudeAccentBg = Color(0xFFFFFBEB)          // amber-50 — фон для выделения
val ClaudeDivider = Color(0xFFE4E4E7)

// ── Семантические цвета ─────────────────────────────────────────────────────
val StatusOk = Color(0xFF16A34A)        // green-600 — активный/оплачено
val StatusOkBg = Color(0xFFDCFCE7)
val StatusOverdue = Color(0xFFDC2626)   // red-600 — долг/просрочка
val StatusOverdueBg = Color(0xFFFEE2E2)
val StatusReturned = Color(0xFF71717A)  // серый — скутер вернули
val StatusReturnedBg = Color(0xFFF4F4F5)
val StatusInfo = Color(0xFF2563EB)      // blue-600
val StatusInfoBg = Color(0xFFDBEAFE)
